import { createRoutine } from "redux-saga-routines";

export const clearDomainTemplates = createRoutine("CLEAR_DOMAIN_TEMPLATES");
export const fetchDomainTemplates = createRoutine("FETCH_DOMAIN_TEMPLATES");
